const sayhi = (name)=>{   //it is a function with parameter name
    console.log(`hello there ${name}`)
    }
    //export default
 module.exports = sayhi   //if we have only one think to export then we can write like that
 